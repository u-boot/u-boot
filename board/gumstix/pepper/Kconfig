if TARGET_PEPPER

config SYS_BOARD
	default "pepper"

config SYS_VENDOR
	default "gumstix"

config SYS_SOC
	default "am33xx"

config SYS_CONFIG_NAME
	default "pepper"

config DM
	default y

config DM_GPIO
	default y

config DM_SERIAL
	default y

endif
