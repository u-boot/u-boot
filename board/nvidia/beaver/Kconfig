if TARGET_BEAVER

config SYS_BOARD
	default "beaver"

config SYS_VENDOR
	default "nvidia"

config SYS_CONFIG_NAME
	default "tegra"

config TEGRA_BOARD_STRING
	string "Default Tegra board name"
	default "NVIDIA Beaver"

endif
