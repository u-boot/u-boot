if TARGET_BEAVER

config SYS_CPU
	string
	default "arm720t" if SPL_BUILD
	default "armv7" if !SPL_BUILD

config SYS_BOARD
	string
	default "beaver"

config SYS_VENDOR
	string
	default "nvidia"

config SYS_SOC
	string
	default "tegra30"

config SYS_CONFIG_NAME
	string
	default "beaver"

endif
