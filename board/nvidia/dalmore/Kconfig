if TARGET_DALMORE

config SYS_CPU
	string
	default "arm720t" if SPL_BUILD
	default "armv7" if !SPL_BUILD

config SYS_BOARD
	string
	default "dalmore"

config SYS_VENDOR
	string
	default "nvidia"

config SYS_SOC
	string
	default "tegra114"

config SYS_CONFIG_NAME
	string
	default "dalmore"

endif
