if TARGET_PCM051

config SYS_BOARD
	default "pcm051"

config SYS_VENDOR
	default "phytec"

config SYS_SOC
	default "am33xx"

config SYS_CONFIG_NAME
	default "pcm051"

config DM
	default y if !SPL_BUILD

config DM_GPIO
	default y if !SPL_BUILD

config DM_SERIAL
	default y if !SPL_BUILD

endif
