if TARGET_THUBAN

config SYS_BOARD
	default "draco"

config SYS_VENDOR
	default "siemens"

config SYS_SOC
	default "am33xx"

config SYS_CONFIG_NAME
	default "draco-thuban"

endif

if TARGET_RASTABAN

config SYS_BOARD
	default "draco"

config SYS_VENDOR
	default "siemens"

config SYS_SOC
	default "am33xx"

config SYS_CONFIG_NAME
	default "draco-rastaban"

endif

if TARGET_ETAMIN

config SYS_BOARD
        default "draco"

config SYS_VENDOR
        default "siemens"

config SYS_SOC
        default "am33xx"

config SYS_CONFIG_NAME
        default "draco-etamin"

endif
